JS Basics
Typescript
Angular Intro
Folder structure  -- Hello world project
Data binding(
    one way - string interpolation,
              property binding,
              event binding 
    two way - ngModel
)   -- id generator, meme generator, movigenerator projects
Directives
   --structural Directives (ngIf,ngSwitch,ngFor) -- modals , movieLibrary
   -- property Directives  (ngClass, ngStyle)  -- dynamic changes to comic library
   -- custom Directives
Pipes   --default , custom pipes -- comic library currency
Components
   -- Nested Components
   -- Parent to Child(@Input, content projection) -- movieLibrary with different movies
   -- Child to Parent(@Output, @ViewChild, template referece variable) -- parent -child communication project
Services
   -- httpClient  -- raju rani messages, random joke generator, aichatbot
   -- CRUD -- basic crud app
   -- httpInterceptor
Routing  -- basic routes, child routing, nested/dynamic routing , lazy loading, auth guards
Forms
   -- template Driven Forms
   -- Model Forms
RxJs
Advanced -- Signals, Hydration etc..,
Real Time Project (Angular 19)