JS Basics
Typescript
Angular Intro
Folder structure
Data binding(
    one way - string interpolation,
              property binding,
              event binding
    two way - ngModel
)
Directives
   --structural Directives (ngIf,ngSwitch,ngFor)
   -- property Directives  (ngClass, ngStyle)
   -- custom Directives
Pipes 
Components
   -- Nested Components
   -- Parent to Child(@Input, content projection)
   -- Child to Parent(@Output, @ViewChild, template referece variable)
Services
   -- httpClient
   -- CRUD
   -- httpInterceptor
Routing
Forms
   -- template Driven Forms
   -- Model Forms
RxJs
Advanced -- Signals, Hydration etc..,
Real Time Project (Angular 19)