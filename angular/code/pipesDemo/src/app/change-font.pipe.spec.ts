import { ChangeFontPipe } from './change-font.pipe';

describe('ChangeFontPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeFontPipe();
    expect(pipe).toBeTruthy();
  });
});
